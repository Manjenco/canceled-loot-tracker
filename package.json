{
  "name": "loot-tracker-monorepo",
  "type": "module",
  "private": true,
  "workspaces": [
    "src/web/client",
    "src/web/server"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "test": "node --test \"test/**/*.test.js\""
  },
  "devDependencies": {
    "better-sqlite3": "^12.8.0"
  }
}
